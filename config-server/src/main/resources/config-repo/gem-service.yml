server:
  port: 8081

spring:
  application:
    name: gem-trading
  datasource:
    url: jdbc:postgresql://localhost:5435/gemdb
    username: gem
    password: gem123
  data:
    redis:
      host: localhost
      port: 6379
  jpa:
    hibernate:
      ddl-auto: update

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
